import { QueryDocument } from "./queryBuilder";
import { CriteriaBuilder } from "./criteriaBuilder";
export declare class UpdateDocumentBuilder extends CriteriaBuilder {
    build(updateDocument: QueryDocument): QueryDocument;
    private _isArray(operator, mapping);
    private _prepareQueryModifier(operator, query, mapping);
    private _prepareSortSpecification(sortSpecification, mapping);
}
