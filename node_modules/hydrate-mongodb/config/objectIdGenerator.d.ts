import { IdentityGenerator } from "./configuration";
export declare class ObjectIdGenerator implements IdentityGenerator {
    constructor();
    generate(): any;
    validate(value: any): boolean;
    fromString(text: string): any;
    areEqual(first: any, second: any): boolean;
}
