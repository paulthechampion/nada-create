import { MappingBuilder } from "./mappingBuilder";
export declare class ObjectMappingBuilder extends MappingBuilder {
    protected populateCore(): void;
    private _getInheritedAnnotations(type);
    private _processType(type);
    private _createProperty(symbol);
    private _createPropertyMapping(symbol);
    private _createCollectionMapping(propertyType, mapping);
    private _getPropertyType(symbol);
    private _createEnumMapping(type, annotation);
    private _getMapping(target);
}
