/**
 * @hidden
 */
export declare class Observer {
    private _watching;
    private _onChange;
    /**
     * Creates an Observer object.
     * @param callback Called the first time any of the watched objects change.
     */
    constructor(callback: () => void);
    watch(obj: any): void;
    destroy(): void;
    private _createOnChangeEvent(callback);
}
